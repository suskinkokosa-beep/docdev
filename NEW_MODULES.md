# 🎉 Новые модули: Шаблоны документов и Геолокация объектов

## ✅ Что было добавлено

### 1. 📄 Модуль "Шаблоны документов"

#### Функциональность
- ✅ Создание библиотеки шаблонов документов (.docx, .xlsx, .pdf)
- ✅ Загрузка файлов-шаблонов
- ✅ Привязка к категориям документов
- ✅ Описание и метаданные шаблонов
- ✅ Поддержка переменных в шаблонах (JSON конфигурация)
- ✅ Мягкое удаление (деактивация вместо полного удаления)
- ✅ Быстрое создание документов из шаблонов

#### База данных
```sql
CREATE TABLE document_templates (
  id UUID PRIMARY KEY,
  name TEXT NOT NULL,
  description TEXT,
  category_id UUID REFERENCES document_categories,
  file_name TEXT NOT NULL,
  file_path TEXT NOT NULL,
  file_size INTEGER NOT NULL,
  mime_type TEXT NOT NULL,
  variables JSONB, -- [{ name: "objectName", label: "Название объекта", type: "text" }]
  is_active BOOLEAN DEFAULT true,
  created_by UUID REFERENCES users,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

#### API Endpoints
- `GET /api/templates` - Список активных шаблонов
- `GET /api/templates/:id` - Получение шаблона
- `POST /api/templates` - Создание шаблона (с загрузкой файла)
- `PUT /api/templates/:id` - Обновление шаблона
- `DELETE /api/templates/:id` - Деактивация шаблона
- `POST /api/templates/:id/create-document` - Создание документа из шаблона

#### Frontend
**Страница**: `TemplatesPage.tsx` (роут: `/templates`)

**Возможности:**
- Карточки шаблонов с иконками по типу файла
- Информация о размере файла и количестве переменных
- Модальное окно для создания шаблона
- Выбор категории из списка
- Кнопка "Использовать" для создания документа
- Кнопка удаления шаблона

**UI особенности:**
- Эмодзи-иконки для разных типов файлов (📄 Word, 📊 Excel, 📕 PDF)
- Бейдж с количеством переменных
- Пустое состояние с подсказкой
- Hover-эффекты на карточках

---

### 2. 🗺️ Модуль "Геолокация объектов"

#### Функциональность
- ✅ Добавление координат GPS к объектам
- ✅ Хранение: широта, долгота, высота
- ✅ Текстовый адрес и регион
- ✅ Расчет расстояния между объектами (формула Haversine)
- ✅ Поиск объектов в радиусе
- ✅ Получение геолокации пользователя
- ✅ Расчет расстояния от пользователя до объекта
- ✅ Интеграция с Google Maps

#### База данных
```sql
CREATE TABLE object_locations (
  id UUID PRIMARY KEY,
  object_id UUID UNIQUE REFERENCES objects ON DELETE CASCADE,
  latitude TEXT NOT NULL,
  longitude TEXT NOT NULL,
  altitude TEXT,
  address TEXT,
  region TEXT,
  geo_data JSONB, -- дополнительные геоданные
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

#### API Endpoints
- `GET /api/objects/:id/location` - Получение локации объекта
- `GET /api/objects-map` - Все объекты с локациями
- `GET /api/objects-map/radius?lat=X&lon=Y&radius=10` - Объекты в радиусе
- `POST /api/objects/:id/location` - Создание локации
- `PUT /api/objects/:id/location` - Обновление локации
- `DELETE /api/objects/:id/location` - Удаление локации

#### Frontend
**Страница**: `ObjectsMapPage.tsx` (роут: `/objects-map`)

**Возможности:**
- Фильтры по типу объекта, статусу, поиск по названию
- Отображение координат объектов
- Расчет расстояния от текущей позиции пользователя
- Статусы объектов с цветными бейджами
- Кнопка "Открыть в Google Maps"
- Поддержка геолокации браузера

**UI особенности:**
- Цветные бейджи статусов (🟢 Активный, 🟡 На обслуживании, ⚫ Неактивный)
- Карточки с адресом и регионом
- Автоматический расчет расстояния в км
- Информационный блок о подключении интерактивной карты

---

## 🚀 Как использовать

### Шаблоны документов

#### 1. Создание шаблона
```typescript
// Через UI или API
POST /api/templates
Content-Type: multipart/form-data

{
  file: <файл .docx/.xlsx/.pdf>,
  name: "Акт приема-передачи",
  description: "Стандартный акт для приема-передачи оборудования",
  categoryId: "uuid",
  variables: JSON.stringify([
    { name: "objectName", label: "Название объекта", type: "text" },
    { name: "date", label: "Дата", type: "date" },
    { name: "responsible", label: "Ответственный", type: "text" }
  ])
}
```

#### 2. Использование шаблона
```typescript
// Создание документа из шаблона
POST /api/templates/:id/create-document
Content-Type: application/json

{
  variables: {
    objectName: "КС-01 Московская",
    date: "2025-01-15",
    responsible: "Иванов И.И."
  }
}
```

### Геолокация объектов

#### 1. Добавление координат к объекту
```typescript
POST /api/objects/:id/location
Content-Type: application/json

{
  latitude: "55.751244",
  longitude: "37.618423",
  altitude: "120.5",
  address: "Москва, ул. Ленина, д. 1",
  region: "Московская область",
  geoData: {
    elevation: 120.5,
    accuracy: 10
  }
}
```

#### 2. Поиск объектов в радиусе
```typescript
// Найти все объекты в радиусе 50 км от точки
GET /api/objects-map/radius?lat=55.751244&lon=37.618423&radius=50

// Ответ
[
  {
    object: {
      id: "uuid",
      code: "KS-01",
      name: "Компрессорная станция №1",
      type: "компрессорная станция",
      status: "active"
    },
    location: {
      latitude: "55.755825",
      longitude: "37.617298",
      address: "Москва, ул. Примерная",
      region: "Москва"
    }
  }
]
```

---

## 💡 Улучшения для будущего

### Шаблоны документов

#### Высокий приоритет
1. **Замена переменных в Word/Excel**
   - Установить библиотеку `docxtemplater` для .docx
   - Установить `exceljs` для .xlsx
   - Реализовать автозаполнение шаблонов

2. **Предпросмотр шаблона**
   - Показ превью файла перед использованием
   - Подсветка переменных в шаблоне

3. **Версионирование шаблонов**
   - История изменений шаблонов
   - Откат к предыдущим версиям

#### Средний приоритет
4. **Конструктор шаблонов**
   - Визуальный редактор для создания шаблонов
   - Drag & drop переменных в документ

5. **Автоматические документы**
   - Создание документов по расписанию
   - Триггеры для автоматической генерации

### Геолокация объектов

#### Высокий приоритет
1. **Интерактивная карта**
   ```bash
   npm install react-leaflet leaflet
   npm install -D @types/leaflet
   ```
   - Отображение объектов на карте Leaflet/OpenStreetMap
   - Кластеризация маркеров
   - Popup с информацией об объекте
   - Фильтры по слоям

2. **Маршруты между объектами**
   - Построение маршрута
   - Расчет времени в пути
   - Экспорт маршрута в GPS-навигатор

3. **Геозоны**
   - Определение зон ответственности
   - Уведомления при входе/выходе из зоны
   - Тепловая карта активности

#### Средний приоритет
4. **Оффлайн карты**
   - Кеширование карт для работы без интернета
   - Синхронизация при восстановлении связи

5. **Навигация с учетом дорог**
   - Интеграция с API маршрутизации
   - Учет пробок и ограничений

---

## 📝 Инструкция по развертыванию

### 1. Обновление базы данных
```bash
# Применить новые таблицы
npm run db:push
```

### 2. Добавление навигации
Добавьте ссылки в `AppSidebar.tsx` или навигационное меню:

```tsx
<NavItem href="/templates" icon={FileText}>
  Шаблоны
</NavItem>
<NavItem href="/objects-map" icon={MapPin}>
  Карта объектов
</NavItem>
```

### 3. Права доступа
Убедитесь что пользователи имеют права:
- `documents.create` - для создания шаблонов
- `documents.view` - для просмотра шаблонов
- `objects.view` - для просмотра карты
- `objects.edit` - для добавления координат

### 4. Тестирование

#### Шаблоны
1. Войдите как админ
2. Перейдите на `/templates`
3. Создайте шаблон (загрузите .docx/.xlsx/.pdf)
4. Проверьте отображение шаблона
5. Попробуйте использовать шаблон

#### Геолокация
1. Войдите в систему
2. Перейдите на `/objects-map`
3. Разрешите доступ к геолокации браузера
4. Добавьте координаты к объекту через API или UI
5. Проверьте расчет расстояния
6. Откройте объект в Google Maps

---

## 🎨 Скриншоты и примеры

### Шаблоны документов
```
┌─────────────────────────────────────┐
│ 📄 Шаблоны документов              │
│ Создавайте документы на основе     │
│ готовых шаблонов        [+ Добавить]│
├─────────────────────────────────────┤
│ ┌──────┐  ┌──────┐  ┌──────┐       │
│ │ 📄   │  │ 📊   │  │ 📕   │       │
│ │ Акт  │  │ Отчет│  │ Инстр│       │
│ │      │  │      │  │      │       │
│ │[Использовать] [🗑]│              │
│ └──────┘  └──────┘  └──────┘       │
└─────────────────────────────────────┘
```

### Карта объектов
```
┌─────────────────────────────────────┐
│ 🗺️ Карта объектов                  │
│ Геолокация и навигация             │
├─────────────────────────────────────┤
│ [Поиск...] [Тип▼] [Статус▼]       │
│ 📍 Найдено: 12 из 15               │
├─────────────────────────────────────┤
│ ┌──────────────────┐                │
│ │ КС-01 [🟢Активн] │                │
│ │ 55.75, 37.61     │                │
│ │ 📍 Москва        │                │
│ │ 🧭 ~5.2 км       │                │
│ │ [Открыть в Maps] │                │
│ └──────────────────┘                │
└─────────────────────────────────────┘
```

---

## ✅ Чек-лист готовности

### Backend
- [x] Таблицы в schema.ts
- [x] Методы в storage.ts
- [x] API endpoints в routes.ts
- [x] Проверка прав доступа
- [x] Валидация данных

### Frontend
- [x] TemplatesPage.tsx
- [x] ObjectsMapPage.tsx
- [x] Роуты в App.tsx
- [x] TypeScript интерфейсы
- [x] Error handling
- [x] Loading states
- [x] Empty states

### Документация
- [x] API документация
- [x] Примеры использования
- [x] Инструкция по установке
- [x] Roadmap улучшений

---

## 🎯 Итого

**Добавлено:**
- 2 новые таблицы в БД
- 11 новых API endpoints
- 2 полноценные страницы React
- 20+ методов в storage
- Полная поддержка прав доступа
- Адаптивный UI для mobile/desktop

**Время разработки:** ~4-5 часов для опытного разработчика

**Статус:** ✅ Готово к использованию

**Следующий шаг:** Установить react-leaflet для интерактивной карты:
```bash
npm install react-leaflet leaflet
npm install -D @types/leaflet
```

Затем можно добавить компонент с полноценной картой вместо списка карточек.

---

**Приятной работы с новыми модулями!** 🚀
