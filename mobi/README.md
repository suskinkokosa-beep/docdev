# УправДок Mobile - PWA Application

Progressive Web Application для мобильного доступа к системе управления документацией газопроводов.

## Возможности

- ✅ **Офлайн режим** - работа без подключения к интернету
- 📱 **Мобильная оптимизация** - адаптивный дизайн для смартфонов
- 🔍 **QR-сканер** - сканирование QR-кодов объектов
- 📄 **Просмотр документов** - доступ к документам и объектам
- 💾 **Кэширование данных** - автоматическое сохранение для офлайн доступа
- 🔄 **Автосинхронизация** - обновление данных при подключении

## Технологии

- React 18 + TypeScript
- Vite - быстрая сборка
- Wouter - роутинг
- Service Worker - офлайн режим
- IndexedDB - локальное хранилище
- PWA Manifest - установка как приложение

## Разработка

```bash
cd mobi
npm install
npm run dev
```

Приложение будет доступно на http://localhost:3001

## Сборка

```bash
npm run build
```

Собранное приложение будет в папке `dist/`

## Генерация PWA иконок

Для полной совместимости PWA требуются PNG иконки. Выполните:

```bash
cd mobi
node generate-pwa-icons.js
```

Затем откройте `mobi/generate-icons.html` в браузере и скачайте иконки следующих размеров:
- **192x192** (обязательно)
- **512x512** (обязательно)  
- 72x72, 96x96, 128x128, 144x144, 152x152, 384x384 (опционально)

Сохраните скачанные файлы в `mobi/public/` и обновите `manifest.json`.

**Альтернатива:** используйте онлайн генератор https://www.pwabuilder.com/imageGenerator

## Установка на мобильное устройство

1. Откройте приложение в браузере на мобильном устройстве
2. В меню браузера выберите "Добавить на главный экран"
3. Приложение установится как обычное мобильное приложение

## Офлайн режим

Service Worker автоматически кэширует:
- Статические файлы приложения
- API запросы (с ограниченным временем хранения)
- Загруженные документы

При отсутствии сети приложение использует кэшированные данные.

## Структура

```
mobi/
├── public/          # Статические файлы
│   ├── manifest.json  # PWA манифест
│   └── sw.js         # Service Worker
├── src/
│   ├── components/   # Компоненты
│   ├── pages/       # Страницы приложения
│   ├── lib/         # Утилиты
│   ├── App.tsx      # Главный компонент
│   └── main.tsx     # Точка входа
├── package.json
├── vite.config.ts
└── tsconfig.json
```

## API Proxy

Приложение использует proxy для API запросов:
- `/api/*` → `http://localhost:5000/api/*`
- `/uploads/*` → `http://localhost:5000/uploads/*`

## Тестовый доступ

- **Логин**: admin
- **Пароль**: admin123
