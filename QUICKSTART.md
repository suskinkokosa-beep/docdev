# 🚀 Быстрый старт

## Локальная разработка (Windows/Mac/Linux)

### 1. Установите зависимости

```bash
npm install
```

### 2. Настройте базу данных PostgreSQL

Установите PostgreSQL и создайте базу данных:

```bash
# Windows (скачайте установщик с postgresql.org)
# Mac: brew install postgresql
# Linux: sudo apt-get install postgresql

# Создайте базу данных
sudo -u postgres psql
CREATE DATABASE doc_management;
CREATE USER doc_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE doc_management TO doc_user;
\q
```

### 3. Создайте файл .env

```env
DATABASE_URL=postgres://doc_user:your_password@localhost:5432/doc_management
SESSION_SECRET=your-secret-key-min-32-chars
NODE_ENV=development
PORT=5000
```

### 4. Примените миграции

```bash
npm run db:push
```

### 5. Заполните тестовыми данными

```bash
npm run db:seed
```

### 6. Запустите приложение

```bash
npm run dev
```

Откройте в браузере: http://localhost:5000

**Войдите с данными:**
- Логин: `admin`
- Пароль: `admin123`

## Деплой на сервер Ubuntu 20+

### Вариант 1: Автоматическая установка (рекомендуется)

```bash
# Скопируйте проект на сервер
scp -r ./* user@710945.cloud4box.ru:/home/user/doc-management/

# Подключитесь к серверу
ssh user@710945.cloud4box.ru

# Перейдите в директорию проекта
cd /home/user/doc-management

# Запустите скрипт установки
chmod +x deploy.sh
sudo ./deploy.sh
```

Скрипт автоматически:
- Установит все зависимости
- Настроит PostgreSQL
- Создаст базу данных
- Настроит Nginx
- Создаст systemd сервис
- Настроит автоматическое резервное копирование
- Опционально установит SSL сертификат

### Вариант 2: Ручная установка

См. раздел "Ручная установка" в README.md

## Первые шаги после установки

1. **Смените пароль администратора**
   - Войдите под admin/admin123
   - Перейдите в настройки профиля
   - Смените пароль на безопасный

2. **Создайте УМГ и службы**
   - Перейдите в "Организационная структура"
   - Создайте УМГ (Управления магистральных газопроводов)
   - Добавьте службы к УМГ
   - Создайте подразделения

3. **Настройте роли и права**
   - Перейдите в "Роли и права"
   - Создайте необходимые роли
   - Настройте права для каждой роли

4. **Добавьте пользователей**
   - Перейдите в "Пользователи"
   - Создайте пользователей
   - Назначьте им роли и доступ к УМГ/службам

5. **Добавьте объекты**
   - Перейдите в "Объекты"
   - Создайте объекты (КС, газопроводы и т.д.)
   - QR-коды генерируются автоматически
   - Привяжите объекты к службам

6. **Создайте категории документов**
   - Перейдите в "Документы" → "Категории"
   - Создайте категории (техническая документация, чертежи и т.д.)

7. **Загрузите документы**
   - Перейдите в "Документы" → "Загрузить"
   - Выберите файлы
   - Укажите категорию, объект, теги
   - Настройте права доступа по службам

## Полезные команды

### Разработка
```bash
npm run dev          # Запуск в режиме разработки
npm run build        # Сборка для production
npm run check        # Проверка типов TypeScript
```

### База данных
```bash
npm run db:push      # Применение миграций
npm run db:seed      # Заполнение тестовыми данными
```

### Production (на сервере)
```bash
# Управление сервисом
sudo systemctl status doc-management
sudo systemctl restart doc-management
sudo systemctl stop doc-management

# Просмотр логов
sudo journalctl -u doc-management -f
sudo tail -f /var/log/nginx/doc-management-error.log

# Резервное копирование
sudo /usr/local/bin/backup-doc-management.sh
```

## Структура прав

### Базовые модули
- **users** - Управление пользователями
- **objects** - Управление объектами  
- **documents** - Управление документами
- **orgstructure** - Организационная структура
- **roles** - Управление ролями
- **training** - Обучение
- **audit** - Журнал аудита

### Базовые действия
- **view** - Просмотр
- **create** - Создание
- **edit** - Редактирование
- **delete** - Удаление

Пример: право `users.create` дает возможность создавать пользователей.

## Тестирование API

Вы можете использовать curl или Postman для тестирования API:

```bash
# Вход в систему
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}' \
  -c cookies.txt

# Получение списка объектов
curl -X GET http://localhost:5000/api/objects \
  -b cookies.txt

# Создание УМГ
curl -X POST http://localhost:5000/api/umg \
  -H "Content-Type: application/json" \
  -b cookies.txt \
  -d '{"name":"УМГ Север","code":"UMG-NORTH","description":"Описание"}'
```

## Решение проблем

### Ошибка подключения к БД
```bash
# Проверьте, что PostgreSQL запущен
sudo systemctl status postgresql

# Проверьте DATABASE_URL в .env
cat .env | grep DATABASE_URL
```

### Ошибка "Port already in use"
```bash
# Найдите процесс, использующий порт
lsof -i :5000

# Остановите процесс
kill -9 <PID>
```

### Ошибки при загрузке файлов
```bash
# Проверьте права на директорию uploads
ls -la uploads/
chmod 755 uploads/
```

## Дополнительная документация

- Полная документация: [README.md](README.md)
- Дизайн-гайдлайны: [design_guidelines.md](design_guidelines.md)
- Схема базы данных: [shared/schema.ts](shared/schema.ts)

## Поддержка

При возникновении проблем:
1. Проверьте логи приложения: `sudo journalctl -u doc-management -f`
2. Проверьте логи Nginx: `sudo tail -f /var/log/nginx/doc-management-error.log`
3. Убедитесь, что все сервисы запущены: PostgreSQL, Nginx, doc-management

---

**Готово!** 🎉 Система готова к использованию.
